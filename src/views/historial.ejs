<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Historial de consultas</title>
    <%- include ('partials/head.html') %>

</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-primary bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/"> <img src="https://upload.wikimedia.org/wikipedia/commons/thumb/9/97/Sqlite-square-icon.svg/1200px-Sqlite-square-icon.svg.png" width="30" height="30" alt="logo" loading="lazy"> SQLite - FI UNMDP</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/historial">Mis Consultas</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Cerrar Sesión</a>
                    </li>
                </ul>
            </div>

        </div>
    </nav>

    <div class="container">
        <div class="row">
            <div class="col-sm">
                <h4 class="mt-3 text-secondary"> Consultas realizadas por <span class="font-weight-light">
                        <%= username %>
                    </span> (<%= hist.length %>)</h4>
                <hr />
                <% if(!flag){ %>
                    <h2>No hay resultados </h2>
                <% }else {%>
                <ul class="pl-0">
                    <% for (var i=0; i < hist.length; i++) { %>
                        <li class="card w-100 mb-3">
                            <div class="card-body">
                                <small class="card-text text-secondary"><i class="fa fa-clock mr-1"></i>
                                    <%= hist[i].fecha %>
                                </small>
                                <hr class="mt-0" />
                                <div class="alert alert-warning" role="alert">
                                    <pre><%= hist[i].consulta %></pre>
                                </div>
                                
                                <button class="btn btn-primary" type="button" data-toggle="collapse"
                                    data-target="#<%= i %>" aria-expanded="false"
                                    aria-controls="collapseExample">
                                    Ver Resultados
                                </button>
                                <div class="collapse" id="<%= i %>">
                                    <div class="mt-3">
                                        <pre ><%- hist[i].resultado %></pre >
                                    </div>
                                </div>
                            </div>
                        </li>
                        <% } %>
                </ul>
                <%}%>
            </div>
        </div>
    </div>
</body>

</html>